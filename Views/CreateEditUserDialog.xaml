<Window x:Class="FehlzeitApp.Views.CreateEditUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Benutzer" Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F1F5F9">

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563EB"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#9CA3AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#F8FAFC" Padding="20" CornerRadius="12,12,0,0">
                <StackPanel>
                    <TextBlock x:Name="TxtTitle" Text="➕ Neuer Benutzer" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Benutzerdaten eingeben" FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
                <StackPanel>
                    <!-- Username -->
                    <TextBlock Text="Benutzername *" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtUsername" Margin="0,0,0,16"/>

                    <!-- Email -->
                    <TextBlock Text="E-Mail" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtEmail" Margin="0,0,0,16"/>

                    <!-- First Name -->
                    <TextBlock Text="Vorname" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtFirstName" Margin="0,0,0,16"/>

                    <!-- Last Name -->
                    <TextBlock Text="Nachname" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                    <TextBox x:Name="TxtLastName" Margin="0,0,0,16"/>

                    <!-- Role -->
                    <TextBlock Text="Rolle *" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <RadioButton x:Name="RbUser" Content="Benutzer" IsChecked="True" GroupName="Role"/>
                        <RadioButton x:Name="RbAdmin" Content="Administrator" GroupName="Role"/>
                    </StackPanel>

                    <!-- Status (only for edit mode) -->
                    <StackPanel x:Name="PanelStatus" Visibility="Collapsed" Margin="0,0,0,16">
                        <TextBlock Text="Status" FontWeight="SemiBold" FontSize="14" Foreground="#0F172A" Margin="0,0,0,8"/>
                        <CheckBox x:Name="ChkIsActive" Content="Aktiv"/>
                    </StackPanel>

                    <!-- Info Message -->
                    <Border Background="#DBEAFE" BorderBrush="#3B82F6" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,8,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtInfoMessage" Text="Ein temporäres Passwort wird automatisch generiert." 
                                       FontSize="13" Foreground="#1E40AF" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" Background="#F8FAFC" Padding="20" CornerRadius="0,0,12,12" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Abbrechen" Style="{StaticResource SecondaryButton}" 
                            Click="BtnCancel_Click" HorizontalAlignment="Left" MinWidth="120"/>
                    
                    <Button x:Name="BtnSave" Grid.Column="1" Content="Erstellen" Style="{StaticResource PrimaryButton}" 
                            Click="BtnSave_Click" MinWidth="120"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
