<Window x:Class="FehlzeitApp.Views.FehlzeitDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FehlzeitApp.Views"
        Title="Fehlzeit bearbeiten"
        Height="700"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Color to Brush Converter -->
        <local:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        
        <!-- EXACTLY matching the Login Page Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderThickness="0">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.15" Color="#000"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6366F1"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4F46E5"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#94A3B8"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- EXACTLY matching the Login Page Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12"
                                    Padding="16,14">
                                <ScrollViewer x:Name="PART_ContentHost"/>
                            </Border>
                            <TextBlock x:Name="placeholder"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="#9CA3AF"
                                       FontSize="14"
                                       IsHitTestVisible="False"
                                       Margin="18,14,18,14"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8B5CF6"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#C4B5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Simple ComboBox style with login page colors -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Grid.Column="2"
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="12">
                                            <Grid>
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" 
                                                                Margin="16,14"/>
                                                <Path x:Name="arrow" HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      Data="M0,0 L4,4 L8,0 Z" Fill="#6B7280" 
                                                      Margin="0,0,16,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#C4B5FD"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#8B5CF6"/>
                                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="16,14,40,14"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                   Style="{x:Null}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Margin="16,14,40,14"
                                   Focusable="True"
                                   Background="Transparent"
                                   BorderThickness="0"
                                   Visibility="Hidden"
                                   IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                          Background="White"
                                          BorderThickness="1.5"
                                          BorderBrush="#E5E7EB"
                                          CornerRadius="12"
                                          Margin="0,4,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Color="#000"/>
                                        </Border.Effect>
                                    </Border>
                                    <ScrollViewer Margin="8,8,8,8" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#C4B5FD"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#8B5CF6"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Simple DatePicker style with login page colors -->
        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="FontSize" Value="16"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#C4B5FD"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#8B5CF6"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                BorderThickness="1.5"
                                BorderBrush="#E5E7EB">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F9FAFB"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#D1D5DB"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- EXACTLY matching the Login Page Glassmorphism Background -->
        <Border CornerRadius="24"
                Background="#F8FAFC"
                BorderThickness="1"
                BorderBrush="#E2E8F0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.15" Color="#000"/>
            </Border.Effect>
        </Border>

        <!-- EXACTLY matching the Login Page Close Button -->
        <Button x:Name="btnClose"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Width="32"
                Height="32"
                Margin="0,16,16,0"
                Background="Transparent"
                BorderThickness="0"
                FontFamily="Segoe MDL2 Assets"
                Content="&#xE8BB;"
                Foreground="#94A3B8"
                FontSize="12"
                Cursor="Hand"
                Click="BtnClose_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        CornerRadius="16"
                                        BorderThickness="0">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FEF2F2"/>
                            <Setter Property="Foreground" Value="#EF4444"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Main Content - EXACTLY matching Login Page layout -->
        <StackPanel Margin="40,60,40,40"
                    VerticalAlignment="Center"
                    KeyboardNavigation.TabNavigation="Cycle">

            <!-- Logo/Brand Section - EXACTLY matching Login Page -->
            <Border Width="80"
                    Height="80"
                    Background="#8B5CF6"
                    CornerRadius="20"
                    Margin="0,0,0,32"
                    HorizontalAlignment="Center">
                <TextBlock Text="📅"
                           FontSize="32"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- Header - EXACTLY matching Login Page -->
            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,32">
                <TextBlock Text="Fehlzeit erfassen"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#1E293B"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock Text="Dokumentieren Sie Ihre Abwesenheit"
                           FontSize="16"
                           Foreground="#64748B"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                
                <!-- Selected Employee Display -->
                <Border Background="#EEF2FF" 
                        BorderBrush="#C7D2FE" 
                        BorderThickness="1" 
                        CornerRadius="8" 
                        Padding="12,8" 
                        Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="👤" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Ausgewählter Mitarbeiter: " 
                                   FontSize="14" 
                                   FontWeight="Medium"
                                   Foreground="#374151"/>
                        <TextBlock x:Name="txtSelectedEmployee" 
                                   Text="Kein Mitarbeiter ausgewählt" 
                                   FontSize="14" 
                                   FontWeight="SemiBold"
                                   Foreground="#2563EB"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Form Fields - Two Column Layout -->
            <Grid Margin="0,0,0,32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Row 0: Von Datum and Bis Datum -->
                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Von Datum"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="#374151"
                               Margin="4,0,0,8"/>
                    <DatePicker x:Name="dpVon"
                               Style="{StaticResource ModernDatePicker}"
                               Height="52"
                               SelectedDateFormat="Short"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Text="Bis Datum"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="#374151"
                               Margin="4,0,0,8"/>
                    <DatePicker x:Name="dpBis"
                               Style="{StaticResource ModernDatePicker}"
                               Height="52"
                               SelectedDateFormat="Short"/>
                </StackPanel>

                <!-- Row 1: Art der Krankheit and Art der Meldung -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,16,0,0">
                    <TextBlock Text="Art der Krankheit"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="#374151"
                               Margin="4,0,0,8"/>
                    <ComboBox x:Name="cmbKrankheit"
                              Style="{StaticResource ModernComboBox}"
                              Height="52">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <TextBlock Text="{Binding Kurz}" 
                                               FontWeight="Bold"
                                               VerticalAlignment="Center"
                                               FontSize="12"
                                               MinWidth="40"/>
                                    <TextBlock Text=" - " 
                                               VerticalAlignment="Center"
                                               FontSize="12"
                                               Margin="5,0"/>
                                    <TextBlock Text="{Binding Beschreibung}" 
                                               VerticalAlignment="Center"
                                               FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,16,0,0">
                    <TextBlock Text="Art der Meldung"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="#374151"
                               Margin="4,0,0,8"/>
                    <ComboBox x:Name="cmbMeldung"
                              Style="{StaticResource ModernComboBox}"
                              Height="52">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5">
                                    <Border Width="20" Height="15" 
                                            Background="{Binding FarbeColor, Converter={StaticResource ColorToBrushConverter}}"
                                            BorderBrush="Gray" 
                                            BorderThickness="1" 
                                            Margin="0,0,8,0"
                                            CornerRadius="2"/>
                                    <TextBlock Text="{Binding Beschreibung}" 
                                               VerticalAlignment="Center"
                                               FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

            </Grid>

            <!-- Action Buttons Section - At Bottom -->
            <Border Background="White"
                    CornerRadius="16"
                    Padding="32,24"
                    Margin="0,32,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.08" Color="#000"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="btnCancel"
                            Style="{StaticResource SecondaryButton}"
                            Background="#FFFFFF"
                            Foreground="#6B7280"
                            FontSize="16"
                            FontWeight="Medium"
                            Padding="24,0"
                            Height="52"
                            Width="140"
                            Content="Abbrechen"
                            Click="BtnCancel_Click"
                            Margin="0,0,16,0"/>

                    <Button x:Name="btnSave"
                            Style="{StaticResource ModernButton}"
                            Background="#7C3AED"
                            Foreground="White"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Padding="24,0"
                            Height="52"
                            Width="140"
                            IsDefault="True"
                            Click="BtnSave_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Speichern"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Window>